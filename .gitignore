# AI and internal docs
GOAL.md 

# Local environment and secrets: only the template is committed.
environments/*
!environments/.env.example
.env
*.env
!*.env.example

# Downloaded and generated data
data/cache/
*.osm.pbf
*.pbf.part

# Terraform local artifacts (state lives in a Docker volume; the lock file is committed)
terraform/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.tfplan
crash.log

# Editor, OS and local tooling
.claude/
.vscode/
.idea/
*.swp
.DS_Store
